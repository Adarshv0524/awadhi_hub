---
export const prerender = false;

const { id } = Astro.params;
const url = new URL(Astro.request.url);
const query = url.search || "";

return Astro.redirect(`/poetry/${encodeURIComponent(String(id || ""))}${query}`, 301);
---
