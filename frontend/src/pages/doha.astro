---
const url = new URL(Astro.request.url);
const params = new URLSearchParams(url.searchParams);

// Preserve existing query params while forcing the polymorphic doha filter.
params.set("type", "doha");
params.set("poetry_type", "doha");

return Astro.redirect(`/poetry?${params.toString()}`, 301);
---